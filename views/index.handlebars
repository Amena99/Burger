<div class="container-index">
<div class="row">
  <div class="col-sm-4 list">
    <h3>Burgers Not Devoured!</h3>
      <ul id="burgers-list">
      {{#each burgers}}
        {{#unless devoured}}
          {{> burgers/burger-block beDevoured=true}}
        {{/unless}}
      {{/each}}
      </ul>
  </div>

  <div class="col-md-4">
    <div class="row">
      <div class="col-md-12">
    <h3>Add a Burger</h3>
      <form class="create-form">

        <div class="form-group">
          <label for="burger-name">Burger Name:</label>
          <input type="text" id="burger-name" name="name">
        </div>

        <div class="form-group">
          <label for="devoured">Devoured?</label><br>
          <input type="radio" name="devoured" id="devoured" value="1"> Devoured!<br>
          <input type="radio" name="notdevoured" value="0"> Not Devoured
        </div>

      <button class="submit-burger btn btn-warning" type="submit">Add Burger</button>
    </form> 
    </div>

    {{!-- Update Burger --}}
    {{!-- <div class="col-md-6">
      <h2>Update a Burger</h2>
        <form id="updateburger" class="button-size">
          <select name="id">
	        	{{#each burgers}}
			      <option value="{{id}}">{{burger_name}}</option>
		        {{/each}}
	        </select>
          <textarea type="text" name="burger" placeholder="Updated Burger"></textarea>
          <button type="submit">Update Burger!</button>
        </form>
      </div> --}}
    </div>
  </div>

<div class="col-sm-4 list">
<h3>Burgers devoured!</h3>
<ul id="burgers-list">
  {{#each burgers}}
    {{#if devoured}}
     {{> burgers/burger-block beDevoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>

{{!-- <div class="col-lg-4">

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger_name">Burger Name:</label>
    <input type="text" id="burger_name" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1"> Devoured!<br>
    <input type="radio" name="notdevoured" value="0"> Not Devoured
  </div>

  <button type="submit">Add Burger</button>
</form>  --}}

{{!-- </div> --}}
</div>
</div>